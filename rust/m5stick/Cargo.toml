[package]
edition      = "2024"
name         = "m5stick"
rust-version = "1.95"
version      = "0.1.0"

[[bin]]
name = "m5stick"
path = "./src/main.rs"

[dependencies]
esp-hal = { version = "~1.1.0", features = ["esp32", "unstable"] }


esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32"] }

critical-section = "1.2.0"
esp-println = { version = "0.17.0", features = ["esp32"] }
embedded-graphics = "0.8.2"
embedded-hal-bus = "0.3.0"
mipidsi = "0.10.0"
embedded-hal = "1"
embassy-executor = "0.10.0"
embassy-futures = "0.1"
embassy-time = "0.5.0"
esp-alloc = "0.10.0"
esp-radio = { version = "0.18.0", features = ["esp32", "ble", "unstable"] }
esp-rtos = { version = "0.3.0", features = ["esp32", "embassy", "esp-alloc", "esp-radio"] }
trouble-host = { version = "0.6.0", features = ["security"] }
embassy-sync = "0.7"
static_cell = "2"


# For fine tuning these settings, please refer to https://doc.rust-lang.org/cargo/reference/profiles.html
[profile.dev]
# The default debug profile is too slow and too big for resource-constrained devices.
# Always build with some optimizations enabled.
opt-level = "s"

[profile.release]
codegen-units = 1     # LLVM can perform better optimizations using a single thread
debug         = 2     # prefer slower builds but better debugging experience
lto           = 'fat'
opt-level     = 's'
